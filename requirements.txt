# full dev environment: the base requirements plus the 'encode' extra (see setup.py).
# the 'anserini' and 'seismic' extras are not needed for tests, which stub the native packages.
ir_datasets
numpy
tqdm
trecrun~=0.3.0
datasets
safetensors
torch
transformers
